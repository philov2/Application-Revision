"use client"; import { useEffect, useRef, useState } from "react"; import Navbar from "@/components/Navbar"; import DemoBanner from "@/components/DemoBanner"; import DevoirCard from "@/components/DevoirCard"; import AuthGuard from "@/components/AuthGuard"; import MatiereDocuments from "@/components/MatiereDocuments"; import MessagerieFamille from "@/components/MessagerieFamille"; import { supabase, supabaseConfigured } from "@/lib/supabaseClient"; import { authFetch } from "@/lib/authFetch"; import { enfants as enfantsDemo, devoirsEnfant, matieres as matieresDemo } from "@/lib/sampleData"; import StatsDevoirs from "@/components/StatsDevoirs"; import { filtrerDevoirsFaitsRecents, filtrerDevoirsEnAttenteCorrection, filtrerDevoirsArchives } from "@/lib/devoirsStats"; import { chargerDevoirs } from "@/lib/devoirsSupabase"; import { compterNonLus } from "@/lib/messagesSupabase"; import { envoyerNotification } from "@/lib/notifications"; import { choisirCouleurMatiere } from "@/lib/couleursMatieres"; import FormulaireDevoir from "@/components/FormulaireDevoir"; export default function DashboardParent() { return ( <AuthGuard role="parent"> <Contenu /> </AuthGuard> ); } function Contenu() { const [enfants, setEnfants] = useState(supabaseConfigured ? [] : enfantsDemo); const [matieres, setMatieres] = useState(supabaseConfigured ? [] : matieresDemo); const [enfantSelectionne, setEnfantSelectionne] = useState(enfantsDemo[0]?.id); const [formEnfantOuvert, setFormEnfantOuvert] = useState(false); const [formSoutienOuvert, setFormSoutienOuvert] = useState(false); const [formCoparentOuvert, setFormCoparentOuvert] = useState(false); const [nouvelleMatiereOuvert, setNouvelleMatiereOuvert] = useState(false); const [nomNouvelleMatiere, setNomNouvelleMatiere] = useState(""); const [enCoursMatiere, setEnCoursMatiere] = useState(false); const [message, setMessage] = useState(""); const [nomParent, setNomParent] = useState(""); const [compteId, setCompteId] = useState(null); const [devoirs, setDevoirs] = useState(supabaseConfigured ? [] : devoirsEnfant); /* Jalon "onglet Gestion du compte" (signalement de Phil : regrouper nouvel */ /* enfant, nouveau soutien et invitation co-parent dans un onglet dedie, */ /* separe des devoirs/documents/messages, pour simplifier l'ecran). */ /* Jalon "repertoire d'archivage" (signalement de Phil : les devoirs faits */ /* trop anciens pour rester dans l'onglet Devoirs, et les documents */ /* archives manuellement depuis l'onglet Documents, se retrouvent dans un */ /* onglet dedie plutot que de disparaitre). Place en dernier dans la barre */ /* d'onglets (signalement de Phil), apres Gestion du compte. */ const [onglet, setOnglet] = useState("devoirs"); /* "devoirs" | "documents" | "messages" | "compte" | "archives" */ const [nonLus, setNonLus] = useState(0); const nonLusRef = useRef(null); useEffect(() => { if (!supabaseConfigured) return; (async () => { const { data: { session } } = await supabase.auth.getSession(); if (!session) return; const { data: compte } = await supabase.from("comptes").select("nom, role").eq("id", session.user.id).single(); if (compte?.nom) setNomParent(compte.nom); setCompteId(session.user.id); if (compte?.role === "admin") { const { data: tousEnfants } = await supabase.from("comptes").select("id, nom").eq("role", "enfant").order("nom"); const listeEnfants = (tousEnfants || []).map((e) => ({ id: e.id, nom: e.nom, niveau: "", devoirsAFaire: 0, devoirsFaits: 0, })); if (listeEnfants.length > 0) { setEnfants(listeEnfants); setEnfantSelectionne(listeEnfants[0].id); await recharger(listeEnfants[0].id); } const { data: mats } = await supabase.from("matieres").select("id, nom, couleur").order("nom"); if (mats) setMatieres(mats); return; } const { data: liens } = await supabase .from("liens_parent_enfant") .select("enfant_id, niveau_scolaire, enfant:comptes!enfant_id (id, nom)") .eq("parent_id", session.user.id); const listeEnfants = (liens || []).map((l) => ({ id: l.enfant?.id, nom: l.enfant?.nom, niveau: l.niveau_scolaire, devoirsAFaire: 0, devoirsFaits: 0, })); if (listeEnfants.length > 0) { setEnfants(listeEnfants); setEnfantSelectionne(listeEnfants[0].id); await recharger(listeEnfants[0].id); } const { data: mats } = await supabase.from("matieres").select("id, nom, couleur").order("nom"); if (mats) setMatieres(mats); })(); }, []); const enfant = enfants.find((e) => e.id === enfantSelectionne) || enfants[0]; async function recharger(id) { const liste = await chargerDevoirs(id); setDevoirs(liste); } useEffect(() => { if (!supabaseConfigured || !enfant?.id || !compteId) { setNonLus(0); return; } let annule = false; nonLusRef.current = null; async function rafraichirNonLus() { try { const n = await compterNonLus(enfant.id, compteId); if (!annule) { if (nonLusRef.current !== null && n > nonLusRef.current) { envoyerNotification("Nouveau message", `Vous avez un nouveau message — ${enfant.nom}.`); } nonLusRef.current = n; setNonLus(n); } } catch { /* ignore */ } } rafraichirNonLus(); const intervalle = setInterval(rafraichirNonLus, 15000); return () => { annule = true; clearInterval(intervalle); }; }, [enfant?.id, compteId, onglet]); async function ajouterEnfant(e) { e.preventDefault(); setMessage(""); const form = new FormData(e.target); try { if (supabaseConfigured) { await authFetch("/api/enfants", { method: "POST", body: JSON.stringify({ nom: form.get("nom"), email: form.get("email"), niveau_scolaire: form.get("niveau_scolaire"), telephone: form.get("telephone"), }), }); setMessage("Compte enfant créé — un email d'invitation a été envoyé."); } else { setMessage("Mode démonstration : aucune donnée réelle n'est créée (connectez Supabase, voir README)."); } setFormEnfantOuvert(false); e.target.reset(); } catch (err) { setMessage(err.message); } } async function demanderSoutien(e) { e.preventDefault(); setMessage(""); const form = new FormData(e.target); const matieresChoisies = form.getAll("matieres"); try { if (supabaseConfigured) { const { data: { session } } = await supabase.auth.getSession(); const { error } = await supabase.from("demandes_comptes").insert({ type_compte: "soutien", nom: form.get("nom"), email: form.get("email"), telephone: form.get("telephone"), matieres: matieresChoisies, demandeur_id: session.user.id, }); if (error) throw error; setMessage("Demande envoyée à l'administrateur pour validation."); } else { setMessage("Mode démonstration : aucune donnée réelle n'est créée (connectez Supabase, voir README)."); } setFormSoutienOuvert(false); e.target.reset(); } catch (err) { setMessage(err.message); } } /* Jalon "compte co-parent" (signalement de Phil : deux parents distincts */ /* doivent pouvoir suivre le meme enfant) : demande soumise a validation par */ /* l'administrateur, comme pour un soutien. A la validation, le nouveau */ /* compte est rattache automatiquement a TOUS les enfants du demandeur (voir */ /* app/api/demandes/[id]/valider/route.js). */ async function demanderCoparent(e) { e.preventDefault(); setMessage(""); const form = new FormData(e.target); try { if (supabaseConfigured) { const { data: { session } } = await supabase.auth.getSession(); const { error } = await supabase.from("demandes_comptes").insert({ type_compte: "coparent", nom: form.get("nom"), email: form.get("email"), telephone: form.get("telephone"), demandeur_id: session.user.id, }); if (error) throw error; setMessage("Demande envoyée à l'administrateur pour validation."); } else { setMessage("Mode démonstration : aucune donnée réelle n'est créée (connectez Supabase, voir README)."); } setFormCoparentOuvert(false); e.target.reset(); } catch (err) { setMessage(err.message); } } async function creerNouvelleMatiere(e) { e.preventDefault(); const nomMatiereSaisi = nomNouvelleMatiere.trim(); if (!nomMatiereSaisi) return; setMessage(""); if (matieres.some((m) => m.nom.trim().toLowerCase() === nomMatiereSaisi.toLowerCase())) { setMessage("Cette matière existe déjà."); return; } setEnCoursMatiere(true); try { const couleur = choisirCouleurMatiere(nomMatiereSaisi, matieres); const { data, error } = await supabase.from("matieres").insert({ nom: nomMatiereSaisi, couleur }).select().single(); if (error) throw error; setMatieres((prev) => [...prev, data].sort((a, b) => a.nom.localeCompare(b.nom))); setNomNouvelleMatiere(""); setNouvelleMatiereOuvert(false); } catch (err) { setMessage(err.message); } finally { setEnCoursMatiere(false); } } function retirerMatiereLocale(id) { setMatieres((prev) => prev.filter((m) => m.id !== id)); } /* Renommage d'une matiere (corrige une erreur de frappe) : met a jour la liste locale sans recharger toute la page, comme retirerMatiereLocale ci-dessus pour la suppression. */ function renommerMatiereLocale(id, nom) { setMatieres((prev) => prev.map((m) => (m.id === id ? { ...m, nom } : m)).sort((a, b) => a.nom.localeCompare(b.nom))); } const devoirsAFaireTries = [...devoirs].filter((d) => d.statut === "a_faire").sort((a, b) => a.echeance.localeCompare(b.echeance)); const devoirsACorriger = filtrerDevoirsEnAttenteCorrection(devoirs); const devoirsFaits = filtrerDevoirsFaitsRecents(devoirs); const devoirsArchivesListe = filtrerDevoirsArchives(devoirs); return ( <> <DemoBanner /> <Navbar role="parent" nom={nomParent || "Parent"} enfantId={enfant?.id} compteId={compteId} /> <main className="flex-1 max-w-3xl w-full mx-auto px-4 py-8 space-y-6"> {message && <p className="text-sm rounded-lg bg-slate-100 dark:bg-slate-800 px-3 py-2">{message}</p>} {enfants.length > 0 && ( <div className="flex gap-2 flex-wrap"> {enfants.map((e) => ( <button key={e.id} onClick={() => { setEnfantSelectionne(e.id); if (supabaseConfigured) recharger(e.id); }} className={`px-4 py-2 rounded-lg text-sm font-medium border ${e.id === enfantSelectionne ? "border-transparent text-white" : "border-slate-300 dark:border-slate-600"}`} style={e.id === enfantSelectionne ? { background: "#4169E1" } : {}} > {e.nom}{e.niveau ? ` · ${e.niveau}` : ""} </button> ))} </div> )} <div className="flex gap-2 border-b border-slate-200 dark:border-slate-700 flex-wrap"> <button onClick={() => setOnglet("devoirs")} className={`px-4 py-2 text-sm font-medium border-b-2 -mb-px ${onglet === "devoirs" ? "border-slate-900 dark:border-white" : "border-transparent text-slate-500 dark:text-slate-400"}`}> Devoirs{enfant ? ` de ${enfant.nom}` : ""} </button> <button onClick={() => setOnglet("documents")} className={`px-4 py-2 text-sm font-medium border-b-2 -mb-px ${onglet === "documents" ? "border-slate-900 dark:border-white" : "border-transparent text-slate-500 dark:text-slate-400"}`}> Chapitres et documents </button> <button onClick={() => setOnglet("messages")} className={`px-4 py-2 text-sm font-medium border-b-2 -mb-px flex items-center gap-1.5 ${onglet === "messages" ? "border-slate-900 dark:border-white" : "border-transparent text-slate-500 dark:text-slate-400"}`}> Messages {nonLus > 0 && ( <span className="inline-flex items-center justify-center rounded-full bg-red-500 text-white text-[10px] font-bold h-4 min-w-4 px-1"> {nonLus} </span> )} </button> <button onClick={() => setOnglet("compte")} className={`px-4 py-2 text-sm font-medium border-b-2 -mb-px ${onglet === "compte" ? "border-slate-900 dark:border-white" : "border-transparent text-slate-500 dark:text-slate-400"}`}> Gestion du compte </button> <button onClick={() => setOnglet("archives")} className={`px-4 py-2 text-sm font-medium border-b-2 -mb-px ${onglet === "archives" ? "border-slate-900 dark:border-white" : "border-transparent text-slate-500 dark:text-slate-400"}`}> 🗄 Archives </button> </div> {onglet === "devoirs" && enfant && ( <> <StatsDevoirs devoirs={devoirs} /> {devoirsACorriger.length > 0 && ( <section> <h2 className="font-semibold mb-3">À corriger</h2> <div className="space-y-3"> {devoirsACorriger.map((d) => <DevoirCard key={d.id} devoir={d} matieres={matieres} compteId={compteId} enfantId={enfant.id} onChange={() => recharger(enfant.id)} />)} </div> </section> )} <section> <div className="flex items-center justify-between mb-3"> <h2 className="font-semibold">Devoirs de {enfant.nom}</h2> <FormulaireDevoir enfantId={enfant.id} compteId={compteId} matieres={matieres} onCree={() => recharger(enfant.id)} /> </div> <div className="space-y-3"> {devoirsAFaireTries.map((d) => <DevoirCard key={d.id} devoir={d} matieres={matieres} compteId={compteId} enfantId={enfant.id} onChange={() => recharger(enfant.id)} />)} </div> </section> <section> <h2 className="font-semibold mb-3 flex items-center gap-1.5 text-green-700 dark:text-green-400"> <span aria-hidden="true">✓</span> Devoirs faits </h2> <div className="space-y-3"> {devoirsFaits.map((d) => <DevoirCard key={d.id} devoir={d} matieres={matieres} compteId={compteId} enfantId={enfant.id} onChange={() => recharger(enfant.id)} />)} </div> </section> </> )} {onglet === "documents" && enfant && compteId && ( <section> <div className="flex items-center justify-between mb-3"> <h2 className="font-semibold">Chapitres et documents</h2> <button onClick={() => setNouvelleMatiereOuvert((v) => !v)} className="text-sm font-medium rounded-lg px-3 py-1.5 border border-dashed border-slate-400"> + Nouvelle matière </button> </div> {nouvelleMatiereOuvert && ( <form onSubmit={creerNouvelleMatiere} className="flex items-center gap-2 mb-3"> <input value={nomNouvelleMatiere} onChange={(e) => setNomNouvelleMatiere(e.target.value)} placeholder="Nom de la nouvelle matière" className="flex-1 rounded-lg border border-slate-300 dark:border-slate-600 bg-transparent px-3 py-2 text-sm" /> <button type="submit" disabled={enCoursMatiere || !nomNouvelleMatiere.trim()} className="rounded-lg px-3 py-2 text-xs font-medium text-white disabled:opacity-50" style={{ background: "#4169E1" }}> {enCoursMatiere ? "..." : "Ajouter"} </button> </form> )} <div className="space-y-4"> {matieres.map((m) => ( <MatiereDocuments key={m.id} matiere={m} enfantId={enfant.id} compteId={compteId} peutSupprimerMatiere={true} onMatiereSupprimee={retirerMatiereLocale} onMatiereRenommee={renommerMatiereLocale} /> ))} {matieres.length === 0 && <p className="text-slate-500 dark:text-slate-400 text-xs">Aucune matière pour l&apos;instant.</p>} </div> </section> )} {onglet === "messages" && enfant && compteId && ( <section> <MessagerieFamille enfantId={enfant.id} compteId={compteId} titre={`Messages — ${enfant.nom}`} /> </section> )} {onglet === "compte" && ( <> <section> <div className="flex items-center justify-between mb-3"> <h2 className="font-semibold">Enfants</h2> <button onClick={() => setFormEnfantOuvert((v) => !v)} className="text-sm font-medium rounded-lg px-3 py-1.5 border border-dashed border-slate-400"> + Ajouter un enfant </button> </div> {formEnfantOuvert && ( <form onSubmit={ajouterEnfant} className="rounded-xl border border-slate-200 dark:border-slate-700 p-4 space-y-3"> <p className="font-medium text-sm">Nouveau compte enfant</p> <input name="nom" required placeholder="Prénom" className="w-full rounded-lg border border-slate-300 dark:border-slate-600 bg-transparent px-3 py-2" /> <input name="email" type="email" required placeholder="Adresse email" className="w-full rounded-lg border border-slate-300 dark:border-slate-600 bg-transparent px-3 py-2" /> <input name="telephone" type="tel" placeholder="Numéro de téléphone (optionnel)" className="w-full rounded-lg border border-slate-300 dark:border-slate-600 bg-transparent px-3 py-2" /> <input name="niveau_scolaire" required placeholder="Niveau scolaire (ex. 4ème)" className="w-full rounded-lg border border-slate-300 dark:border-slate-600 bg-transparent px-3 py-2" /> <button type="submit" className="rounded-lg px-4 py-2 text-sm font-medium text-white" style={{ background: "#4169E1" }}>Créer le compte</button> </form> )} {enfants.length === 0 && !formEnfantOuvert && ( <p className="text-slate-500 dark:text-slate-400 text-xs">Aucun enfant pour l&apos;instant.</p> )} </section> <section> <div className="flex items-center justify-between mb-3"> <h2 className="font-semibold">Soutiens</h2> <button onClick={() => setFormSoutienOuvert((v) => !v)} className="text-sm font-medium rounded-lg px-3 py-1.5 border border-dashed border-slate-400"> + Demander un compte soutien </button> </div> {formSoutienOuvert && ( <form onSubmit={demanderSoutien} className="rounded-xl border border-slate-200 dark:border-slate-700 p-4 space-y-3"> <input name="nom" required placeholder="Nom du soutien" className="w-full rounded-lg border border-slate-300 dark:border-slate-600 bg-transparent px-3 py-2" /> <input name="email" type="email" required placeholder="Adresse email" className="w-full rounded-lg border border-slate-300 dark:border-slate-600 bg-transparent px-3 py-2" /> <input name="telephone" type="tel" required placeholder="Numéro de téléphone" className="w-full rounded-lg border border-slate-300 dark:border-slate-600 bg-transparent px-3 py-2" /> <div> <p className="text-sm mb-1">Matières concernées</p> <div className="grid grid-cols-2 gap-1"> {matieres.map((m) => ( <label key={m.id} className="flex items-center gap-2 text-sm"> <input type="checkbox" name="matieres" value={m.id} /> {m.nom} </label> ))} </div> </div> <button type="submit" className="rounded-lg px-4 py-2 text-sm font-medium text-white" style={{ background: "#4169E1" }}> Envoyer la demande à l&apos;administrateur </button> </form> )} </section> <section> <div className="flex items-center justify-between mb-3"> <h2 className="font-semibold">Autre parent</h2> <button onClick={() => setFormCoparentOuvert((v) => !v)} className="text-sm font-medium rounded-lg px-3 py-1.5 border border-dashed border-slate-400"> + Inviter un co-parent </button> </div> <p className="text-xs text-slate-500 dark:text-slate-400 mb-3"> Le compte invité sera rattaché, une fois validé par l&apos;administrateur, à tous les enfants déjà liés à votre compte. </p> {formCoparentOuvert && ( <form onSubmit={demanderCoparent} className="rounded-xl border border-slate-200 dark:border-slate-700 p-4 space-y-3"> <input name="nom" required placeholder="Nom de l'autre parent" className="w-full rounded-lg border border-slate-300 dark:border-slate-600 bg-transparent px-3 py-2" /> <input name="email" type="email" required placeholder="Adresse email" className="w-full rounded-lg border border-slate-300 dark:border-slate-600 bg-transparent px-3 py-2" /> <input name="telephone" type="tel" required placeholder="Numéro de téléphone" className="w-full rounded-lg border border-slate-300 dark:border-slate-600 bg-transparent px-3 py-2" /> <button type="submit" className="rounded-lg px-4 py-2 text-sm font-medium text-white" style={{ background: "#4169E1" }}> Envoyer la demande à l&apos;administrateur </button> </form> )} </section> </> )} {onglet === "archives" && enfant && compteId && ( <> <section> <h2 className="font-semibold mb-3">🗄 Devoirs archivés</h2> <div className="space-y-3"> {devoirsArchivesListe.map((d) => <DevoirCard key={d.id} devoir={d} matieres={matieres} compteId={compteId} enfantId={enfant.id} onChange={() => recharger(enfant.id)} />)} {devoirsArchivesListe.length === 0 && <p className="text-slate-500 dark:text-slate-400 text-sm">Aucun devoir archivé pour l&apos;instant.</p>} </div> </section> <section> <h2 className="font-semibold mb-3">🗄 Documents archivés</h2> <div className="space-y-4"> {matieres.map((m) => ( <MatiereDocuments key={m.id} matiere={m} enfantId={enfant.id} compteId={compteId} modeArchive /> ))} </div> </section> </> )} </main> </> ); }
