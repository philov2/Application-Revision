import { NextResponse } from "next/server"; import { supabaseAdmin, supabaseAdminConfigured, getCompteFromToken } from "@/lib/supabaseAdmin"; async function estAutorisePourDocument(compte, document) { if (compte.role === "admin" || document.cree_par === compte.id) return true; if (compte.role === "parent") { const { data } = await supabaseAdmin.from("liens_parent_enfant").select("parent_id").eq("parent_id", compte.id).eq("enfant_id", document.enfant_id).maybeSingle(); return !!data; } if (compte.role === "soutien") { const { data } = await supabaseAdmin.from("liens_soutien").select("soutien_id").eq("soutien_id", compte.id).eq("enfant_id", document.enfant_id).eq("matiere_id", document.matiere_id).maybeSingle(); return !!data; } return false; } /* Supprime définitivement un document : réservé à l'administrateur ou à la */ /* personne qui l'a importé (cree_par). Supprime aussi le fichier associé */ /* dans le Storage. */ /* Ordre volontaire : on supprime d'abord la ligne en base, puis le fichier */ /* dans le Storage. Auparavant l'ordre était inversé, ce qui pouvait supprimer */ /* le fichier du Storage puis échouer sur la suppression en base (par exemple */ /* si un devoir référence encore ce document), laissant une ligne orpheline */ /* pointant vers un fichier disparu. Voir Jalon "suppression d'un document */ /* référencé par un devoir" : devoirs.document_id est désormais en */ /* "on delete set null" côté base, donc la suppression en base réussit même */ /* si un devoir référence encore le document (le devoir perd simplement son */ /* document associé). */ export async function DELETE(request, { params }) { if (!supabaseAdminConfigured) { return NextResponse.json({ error: "Supabase n'est pas encore configuré côté serveur (SUPABASE_SERVICE_ROLE_KEY manquante)." }, { status: 500 }); } const compte = await getCompteFromToken(request); if (!compte || compte.statut !== "actif") { return NextResponse.json({ error: "Non autorisé." }, { status: 403 }); } const { id } = await params; const { data: document, error: documentError } = await supabaseAdmin .from("documents") .select("*") .eq("id", id) .single(); if (documentError || !document) { return NextResponse.json({ error: "Document introuvable." }, { status: 404 }); } if (!(await estAutorisePourDocument(compte, document))) { return NextResponse.json({ error: "Reserve a l'administrateur, a la personne ayant importe ce document, ou a un parent/soutien rattache a l'enfant concerne." }, { status: 403 }); } const { error: deleteError } = await supabaseAdmin.from("documents").delete().eq("id", id); if (deleteError) { return NextResponse.json({ error: `Échec de la suppression du document : ${deleteError.message}` }, { status: 500 }); } const { error: storageError } = await supabaseAdmin.storage.from("documents").remove([document.fichier_url]); if (storageError) { /* La ligne est déjà supprimée en base ; le fichier restera orphelin dans */ /* le Storage mais n'est plus référencé par l'application. On ne fait pas */ /* échouer la requête pour autant : du point de vue de l'utilisateur, le */ /* document a bien été supprimé. */ return NextResponse.json({ success: true, avertissement: `Document supprimé, mais le fichier n'a pas pu être retiré du stockage : ${storageError.message}` }); } return NextResponse.json({ success: true }); } /* Jalon "répertoire d'archivage" (signalement de Phil) : bascule le champ */ /* archive d'un document (true pour l'archiver depuis l'onglet Documents, */ /* false pour le désarchiver si besoin plus tard). Même règle de permission */ /* que la suppression ci-dessus : réservé à l'administrateur ou à la personne */ /* qui a importé le document. Contrairement à DELETE, aucune ligne ni fichier */ /* n'est supprimé — le document réapparait simplement groupé par Matière et */ /* Chapitre dans l'onglet Archives. */ export async function PATCH(request, { params }) { if (!supabaseAdminConfigured) { return NextResponse.json({ error: "Supabase n'est pas encore configuré côté serveur (SUPABASE_SERVICE_ROLE_KEY manquante)." }, { status: 500 }); } const compte = await getCompteFromToken(request); if (!compte || compte.statut !== "actif") { return NextResponse.json({ error: "Non autorisé." }, { status: 403 }); } const { id } = await params; const body = await request.json().catch(() => ({})); if (typeof body.archive !== "boolean") { return NextResponse.json({ error: "Le champ 'archive' (booléen) est requis." }, { status: 400 }); } const { data: document, error: documentError } = await supabaseAdmin .from("documents") .select("cree_par, enfant_id, matiere_id") .eq("id", id) .single(); if (documentError || !document) { return NextResponse.json({ error: "Document introuvable." }, { status: 404 }); } if (!(await estAutorisePourDocument(compte, document))) { return NextResponse.json({ error: "Reserve a l'administrateur, a la personne ayant importe ce document, ou a un parent/soutien rattache a l'enfant concerne." }, { status: 403 }); } const { error: updateError } = await supabaseAdmin.from("documents").update({ archive: body.archive }).eq("id", id); if (updateError) { return NextResponse.json({ error: `Échec de la mise à jour : ${updateError.message}` }, { status: 500 }); } return NextResponse.json({ success: true }); } 
